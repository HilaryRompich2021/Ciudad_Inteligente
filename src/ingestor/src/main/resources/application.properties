spring.application.name=ingestor

# Kafka
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.producer.properties.spring.json.add.type.headers=false
spring.kafka.admin.auto-create=true

# Redis (descomenta si usas Redis)
# spring.redis.host=${SPRING_REDIS_HOST:redis}

# PostgreSQL (descomenta si usas base de datos)
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/ciudades}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

# Topic para eventos estandarizados
app.kafka.topic.events-standardized=t01.events.standardized
